from setuptools import setup, find_packages

setup(
    name='utils',
    version='0.0.1',
    packages=find_packages(),
    license='MIT',
    author='santoroc',
    description='ETL utilities to use with aws.',
    setup_requires=['wheel']
)